global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  # Placeholder for future application metrics.
  # Keep disabled until the backend exposes a reviewed /metrics endpoint
  # that does not include API keys, secrets, account identifiers, or user PII.
  # - job_name: backend
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets:
  #         - backend:8000
